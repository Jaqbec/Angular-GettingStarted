import { ConvertToSpacePipe } from './convert-to-space-pipe.pipe';

describe('ConvertToSpacePipePipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertToSpacePipe();
    expect(pipe).toBeTruthy();
  });
});
